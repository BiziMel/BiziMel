# Tenancy Notes

- Every user must belong to a tenant company.
- Application Admin users can create tenants and manage all companies.
- Company Admin users can manage only users and permissions inside their own company.
- User company values are selected from configured tenants.
- Sharing and assignment lists are constrained to users inside the same company tenant.
