# Package Manifest Notes

This package includes the hosted enterprise application source, templates, static assets, PG Bible template, Render configuration and validation support files.

Excluded from the deployment archive:

- Runtime database and export data under `server_data/`.
- Vendored dependency folders under `vendor/`.
- Python bytecode caches.
- `.DS_Store` metadata files.
